<div class="article">
	
	<div class="container narrow">
		<img src="{{ article.image.src | img_url: 'large' }}">
		<p class="published-on"> {{ article.published_at | date: "%B %d, %Y" }}</p>
		<h4> {{ article.title }}</h4>
		<p class="article-content"> {{ article.content }} </p>
{% comment %}		<p> {% include 'shortcode' load: page.content %} </p>
{% endcomment %}	
	</div>
</div>

<style type="text/css">
	li{
		line-height: 1.4;
		list-style-type: disc;
		font-family: 'HelveticaNeue', 'Helvetica Neue', Helvetica, Arial, sans-serif;
	}
	p{
		font-family: 'HelveticaNeue', 'Helvetica Neue', Helvetica, Arial, sans-serif;
		padding-bottom: 0;
		margin-bottom: 0;
	}
	img{
		padding-bottom: 10px;
		margin-bottom: 0;
		margin-top: 60px;
	}
	h4{
		padding-bottom: 30px;
	}
	.published-on{
		padding: 0;
		font-family: 'HelveticaNeue', 'Helvetica Neue', Helvetica, Arial, sans-serif;
		font-style: italic;
		font-weight: 300;
		padding-bottom: 10px;
</style>